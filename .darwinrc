#!/bin/csh -f

setenv JLAB_ROOT /opt/jlab_software
setenv MU /opt/projects/muEvent

echo "Setting custom GEMC on "`hostname` "(.darwinrc)"
setenv GEMC  /opt/projects/gemc/
setenv BANKS /opt/projects/banks/devel
